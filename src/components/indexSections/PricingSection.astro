---
import Button from '../common/Button.astro';
import Badge from '../common/Badge.astro';
import Icon from '../common/Icon.astro';
---

<section class="pricing" aria-labelledby="pricing-heading">
	<div class="container">
		<header class="section-header">
			<Badge icon="💰" text="Simple Pricing" />
			<h2 id="pricing-heading">Simple, Transparent Pricing</h2>
			<p>Choose the plan that fits your entrepreneurial journey</p>
		</header>

		<div class="pricing-grid" role="table" aria-label="Pricing comparison">
			<!-- Free Plan -->
			<article class="pricing-card" role="gridcell">
				<header class="plan-header">
					<h3>Explorer</h3>
					<div class="price">
						<span class="currency" aria-hidden="true">$</span>
						<span class="amount">0</span>
						<span class="period">/month</span>
					</div>
					<p class="plan-description">Perfect for getting started</p>
				</header>
				<ul class="features-list" aria-label="Explorer plan features">
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>5 business ideas per day</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Basic AI analysis</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Community access</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Email support</span>
					</li>
				</ul>
				<Button href="/signup" variant="secondary" class="plan-button">Get Started</Button>
			</article>

			<!-- Pro Plan -->
			<article class="pricing-card featured" role="gridcell">
				<div class="popular-badge" aria-label="Most popular plan">Most Popular</div>
				<header class="plan-header">
					<h3>Professional</h3>
					<div class="price">
						<span class="currency" aria-hidden="true">$</span>
						<span class="amount">29</span>
						<span class="period">/month</span>
					</div>
					<p class="plan-description">For serious entrepreneurs</p>
				</header>
				<ul class="features-list" aria-label="Professional plan features">
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Unlimited business ideas</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Advanced AI analysis & scoring</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Market trend insights</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Custom filters & alerts</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Priority support</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Export & API access</span>
					</li>
				</ul>
				<Button href="/signup?plan=pro" variant="primary" class="plan-button">Start Free Trial</Button>
			</article>

			<!-- Enterprise Plan -->
			<article class="pricing-card" role="gridcell">
				<header class="plan-header">
					<h3>Enterprise</h3>
					<div class="price">
						<span class="currency" aria-hidden="true">$</span>
						<span class="amount">99</span>
						<span class="period">/month</span>
					</div>
					<p class="plan-description">For teams and agencies</p>
				</header>
				<ul class="features-list" aria-label="Enterprise plan features">
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Everything in Professional</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Team collaboration tools</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Advanced analytics dashboard</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Custom integrations</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>Dedicated account manager</span>
					</li>
					<li>
						<Icon name="check" class="check-icon" size={20} aria-hidden="true" />
						<span>SLA & 24/7 support</span>
					</li>
				</ul>
				<Button href="/contact" variant="secondary" class="plan-button">Contact Sales</Button>
			</article>
		</div>

		<footer class="pricing-footer">
			<p>All plans include a 14-day free trial. No credit card required.</p>
		</footer>
	</div>
</section>

<style lang="scss">
	.pricing {
		padding: 6rem 0;
		background: var(--bg-tertiary);

		.section-header {
			text-align: center;
			margin-bottom: 3rem;

			:global(.badge) {
				margin-bottom: 2rem;
			}

			h2 {
				font-size: 2.5rem;
				font-weight: 700;
				margin-bottom: 1rem;
			}

			p {
				font-size: 1rem;
				color: var(--text-secondary);
			}
		}

		.pricing-grid {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 1.5rem;
			margin-bottom: 2rem;
			
			@media (max-width: 1024px) {
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
			}
		}

		.pricing-card {
			position: relative;
			padding: 1.5rem 1.25rem;
			border-radius: 12px;
			border: 2px solid var(--border-color);
			background: var(--bg-secondary);
			backdrop-filter: blur(10px);
			transition:
				transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
				box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1);
			box-shadow: var(--shadow-lg);
			display: flex;
			flex-direction: column;

			&.featured {
				border-color: var(--accent-primary);
				transform: scale(1.05);
			}

			.popular-badge {
				position: absolute;
				top: -10px;
				left: 50%;
				transform: translateX(-50%);
				background: var(--accent-gradient);
				color: white;
				padding: 0.375rem 1rem;
				border-radius: 12px;
				font-size: 0.75rem;
				font-weight: 600;
			}

			.plan-header {
				text-align: center;
				margin-bottom: 1.5rem;

				h3 {
					font-size: 1.25rem;
					font-weight: 600;
					margin-bottom: 0.75rem;
					color: var(--text-primary);
				}

				.price {
					display: flex;
					align-items: baseline;
					justify-content: center;
					margin-bottom: 0.5rem;

					.currency {
						font-size: 1rem;
						font-weight: 600;
						color: var(--text-secondary);
					}

					.amount {
						font-size: 2.25rem;
						font-weight: 700;
						color: var(--text-primary);
						margin: 0 0.25rem;
					}

					.period {
						font-size: 0.875rem;
						color: var(--text-secondary);
					}
				}

				.plan-description {
					color: var(--text-secondary);
					font-size: 0.875rem;
				}
			}

			.features-list {
				list-style: none;
				margin: 0 0 1.5rem 0;
				padding: 0;
				flex-grow: 1;

				li {
					display: flex;
					align-items: center;
					gap: 0.5rem;
					margin-bottom: 0.75rem;
					color: var(--text-primary);
					font-size: 0.875rem;

					.check-icon {
						color: var(--accent-primary);
						flex-shrink: 0;
						width: 16px;
						height: 16px;
					}

					span {
						flex: 1;
					}
				}
			}

			.plan-button {
				width: 100%;
				justify-content: center;
			}
		}

		.pricing-footer {
			text-align: center;

			p {
				color: var(--text-secondary);
				font-size: 0.875rem;
			}
		}
	}

	// Responsive adjustments
	@media (max-width: 768px) {
		.pricing {
			.pricing-grid {
				grid-template-columns: 1fr;
			}

			.pricing-card.featured {
				transform: none;

				&:hover {
					transform: translateY(-8px);
				}
			}
		}
	}
</style>